const prefix = 'mission';

const Mission: React.FC = () => {
  return(
    <div className={`${prefix}-container`}>
      <div className={`${prefix}-content`}>
        <strong>Passionate</strong> about crafting <strong>delightful</strong> and <strong>functional</strong> experiences and focused on writing <strong>clean</strong> and <strong>efficient</strong> code.
      </div>
    </div>
  )
}

export default Mission;